pluginManagement {
    repositories {
        google()
        mavenCentral()
        maven { url "https://storage.googleapis.com/simplify-communications/maven/" }
        gradlePluginPortal()

    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven{url "https://jitpack.io"}
    }
}
rootProject.name = "Modbus-Android"
include ':app'
